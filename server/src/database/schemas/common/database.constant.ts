export enum REQUEST_STATUS {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}

export enum ATTACHMENT_TYPE {
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
} 